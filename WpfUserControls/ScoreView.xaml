<UserControl x:Class="WpfUserControls.ScoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="DefaultBorder" TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="Black" />
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <Border Style="{StaticResource DefaultBorder}">
        <Label Content="{Binding Path=Score, Mode=OneWay}" ContentStringFormat="Счёт: {0}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="36"/>
        </Border>
    </DockPanel>
</UserControl>
